language: python
python:
  - "2.7"
  - "3.6"

env:
  global:
      - PYTHONPATH=.

install:
  - "pip install coverage"
  - "pip install coveralls"
  - "pip install -r requirements.txt"
  - "pip install -r tests/requirements.txt"

script:
  - "coverage run -m unittest tests.test_python_process.TestLocks.test_assign"

after_success:
  - coveralls
